# PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/nodo

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Snark artifacts path (relative to backend dir or absolute)
ZKEY_PATH=../zk/circuits/build/membership_final.zkey
VERIFICATION_KEY_JSON=../zk/circuits/build/verification_key.json
WASM_PATH=../zk/circuits/build/membership_js/membership.wasm

# Anchoring / Ethereum (zkEVM) config (testnet)
RPC_URL=https://rpc.example-zkevm.test
PRIVATE_KEY=0xYOUR_PRIVATE_KEY_FOR_SUBMITTER
NODOANCHOR_ADDRESS=0xYourNoDoAnchorContractAddress

# App
PORT=4000
BATCH_MIN_VOTES=10       # anchor batch when >= N votes
BATCH_MAX_AGE_SECONDS=300  # or after T seconds
