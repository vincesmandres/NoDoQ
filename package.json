{
  "name": "nodoq",
  "private": true,
  "scripts": {
    "zk:compile": "ts-node zk/scripts/compile.ts",
    "contracts:build": "hardhat compile",
    "contracts:test": "hardhat test",
    "contracts:deploy": "hardhat run scripts/deploy.ts --network sepolia",
    "app:dev": "pnpm --filter app dev",
    "app:build": "pnpm --filter app build",
    "dev:all": "pnpm zk:compile && pnpm contracts:build && pnpm app:dev"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "hardhat": "^2.22.3",
    "typescript": "^5.3.3"
  }
}
